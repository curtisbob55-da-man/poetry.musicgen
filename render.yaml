services:
  - type: web
    name: poetic-music-backend
    env: node
    buildCommand: npm install
    startCommand: npm start
    plan: free
    envVars:
      - key: PORT
        value: 8080
      - key: OPENAI_API_KEY
        sync: false
